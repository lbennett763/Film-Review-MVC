@model IEnumerable<FilmReview_MVC_Website.Models.AgeRating>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AgeRatingName)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AgeRatingName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.AgeRatingID }) |
                @Html.ActionLink("Details", "Details", new { id=item.AgeRatingID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.AgeRatingID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
